{
  "name": "codapllm-tools",
  "private": true,
  "description": "Headless CODAP checks for templates and plugins",
  "scripts": { "load-test": "node load-test.js" },
  "dependencies": { "playwright": "^1.56.0" }
}
